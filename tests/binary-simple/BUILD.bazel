load(
    "@rules_haskell//haskell:defs.bzl",
    "haskell_test",
)

package(default_testonly = 1)

haskell_test(
    name = "binary-simple",
    srcs = ["Main.hs"],
    expected_covered_expressions_percentage = 100,
    tags = ["coverage-compatible"],
    visibility = ["//visibility:public"],
    deps = ["//tests/hackage:base"],
    # bazel build --//haskell:use_worker_setting=True //tests/binary-simple
    # Prints ctx.build_setting_value True
    # Comment the following out and it prints ctx.build_setting_value False
    use_worker = "//haskell:use_worker_setting",
    # Interestingly it stops working if we specify the workspace.
    #use_worker = "@rules_haskell//haskell:use_worker_setting",
)
